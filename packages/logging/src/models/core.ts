export enum TransportType {
  FILE = 'file',
  KAFKA = 'kafka',
  STDOUT = 'stdout',
}

export enum Environment {
  LOCAL = 'local',
  DEV = 'dev',
  QA = 'qa',
  PREPROD = 'preprod',
  PROD = 'prod',
}
